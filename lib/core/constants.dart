import 'package:flutter/material.dart';

const int startingChips = 2000;
const int smallBlind = 25;
const int bigBlind = 50;
const Duration botActionDelay = Duration(milliseconds: 800);

const int minBots = 2;
const int maxBots = 3;

const tableGreen = Color(0xFF0C6A48);
